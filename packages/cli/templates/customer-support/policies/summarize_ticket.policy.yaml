apiVersion: policy.akcp.dev/v1alpha1
kind: PolicyCard
metadata:
  name: "summarize_ticket"
  description: "Allows the agent to summarize tickets for human handoff"
appliesTo:
  capabilities:
    - customer-support.summarize_ticket
  riskLevels: ["low"]
rules:
  - effect: allow
