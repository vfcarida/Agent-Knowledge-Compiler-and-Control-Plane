apiVersion: policy.akcp.dev/v1alpha1
kind: PolicyCard
metadata:
  name: "draft_response"
  description: "Allows the agent to draft responses but not send them directly to the customer"
appliesTo:
  capabilities:
    - customer-support.draft_response
  riskLevels: ["low"]
rules:
  - effect: allow
