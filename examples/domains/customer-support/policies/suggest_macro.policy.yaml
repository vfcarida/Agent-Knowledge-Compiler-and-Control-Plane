apiVersion: policy.akcp.dev/v1alpha1
kind: PolicyCard
metadata:
  name: "suggest_macro"
  description: "Allows the agent to suggest a macro for the agent to use"
appliesTo:
  capabilities:
    - customer-support.suggest_macro
  riskLevels: ["low"]
rules:
  - effect: allow
