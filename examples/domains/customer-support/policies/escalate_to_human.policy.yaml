apiVersion: policy.akcp.dev/v1alpha1
kind: PolicyCard
metadata:
  name: "escalate_to_human"
  description: "Allows the agent to escalate the ticket to a human queue"
appliesTo:
  capabilities:
    - customer-support.escalate_ticket
  riskLevels: ["medium"]
rules:
  - effect: allow
