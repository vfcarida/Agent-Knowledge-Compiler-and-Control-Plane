# Career Adapter

**Status:** Active

This template represents the baseline reference implementation of AKCP, demonstrating the Agent Knowledge Compiler and Control Plane (AKCP) (OKF). It includes models for profiles, experiences, preferences, and automated job applications.

To use this profile natively, you can view the `packages/core` and `packages/mcp-automation-server` directories which are built around these primitives.
