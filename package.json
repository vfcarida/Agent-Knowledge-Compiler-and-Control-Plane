{
  "name": "open-career-format-orchestrator",
  "version": "0.1.0",
  "private": true,
  "description": "A Privacy-by-Design career management system combining OKF (Open Knowledge Format) with MCP (Model Context Protocol) and a React dashboard.",
  "author": "Vinicius Carida",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "test:watch": "pnpm -r run test:watch",
    "lint": "pnpm -r run lint",
    "clean": "pnpm -r run clean"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "@types/node": "^22.0.0",
    "vitest": "^3.2.0",
    "prettier": "^3.5.0"
  }
}
